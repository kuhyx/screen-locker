__pycache__/
*.py[cod]
.Python
build/
dist/
*.egg-info/
.env
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.lcov
htmlcov/
*.log
.DS_Store

# Runtime state files — personal workout/health data, never public.
screen_locker/log.json
# ...and the ledger naming which of its entries record the same workout.
screen_locker/duplicate_credits.json
# ...including the timestamped backups a correction script leaves behind.
screen_locker/*.json.bak-*
screen_locker/sick_history.json
screen_locker/extra_benefits_state.json
screen_locker/scheduled_skips.json
screen_locker/shutdown_base.json
screen_locker/sick_day_state.json
screen_locker/early_bird_pending.json

# ci_mirror.sh cached requirements.txt venv (rebuilt on demand)
.ci-mirror-venv/

# Web status UI (web/) build and tooling output.
web/node_modules/
web/dist/
web/coverage/
web/node_modules
web/dist
